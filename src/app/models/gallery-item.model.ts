// This defines how each gallery image object should look
export interface GalleryItem {
    src: string;   // path or URL of the image
    alt?: string;  // optional alt text for accessibility
  }
  